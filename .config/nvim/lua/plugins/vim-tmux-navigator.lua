return {
	"christoomey/vim-tmux-navigator",
	-- Must load at startup: this Vimscript plugin sets global <C-h/j/k/l>
	-- mappings via its plugin/ directory. Lazy-loading on keys breaks it.
	lazy = false,
}
