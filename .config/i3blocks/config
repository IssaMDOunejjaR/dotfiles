# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# background
# border
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
separator_block_width=15
markup=none

# Volume indicator
#
# The first parameter sets the step (and units to display)
# The second parameter overrides the mixer selection
# See the script for details.
[volume]
label=  
command=scripts/volume
instance=Master
interval=once
signal=10

# Network interface monitoring
[network]
command=scripts/internet
interval=10

[bluetooth]
label=
command=scripts/toggle
interval=once
COMMAND_ON=bluetoothctl power on
COMMAND_OFF=bluetoothctl power off
COMMAND_STATUS=bluetoothctl show | grep -q "Powered: yes"

# [bluetooth]
# command=scripts/bluetooth
# interval=15
# label=

# [wifi]
# #instance=wlp3s0
# interval=10

# Connection
# [ssid]
# command=scripts/ssid
#INTERFACE=wlan0
# interval=60

# Battery indicator
[battery]
command=scripts/battery
interval=10

[calendar]
command=scripts/calendar
interval=1
LABEL= 
HEIGHT=200
WIDTH=250

[layout]
label=󰌌 
command=scripts/keyboard
interval=once
